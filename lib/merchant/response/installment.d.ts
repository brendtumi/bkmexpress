export declare class Installment {
    private numberOfInstallment;
    private installmentAmount;
    private totalAmount;
    private label;
    private vposConfig;
    constructor(numberOfInstallment: string, installmentAmount: string, totalAmount: string, vposConfig: string);
    NumberOfInstallment: string;
    InstallmentAmount: string;
    TotalAmount: string;
    VposConfig: string;
    Label: string;
}
