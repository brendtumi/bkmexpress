export declare class Token {
    private shortId;
    private path;
    private token;
    constructor(shortId: string, path: string, token: string);
    ShortId: string;
    Path: string;
    Token: string;
}
